export const metadata = {
  title: 'Authentication | CaaS Platform',
  description: 'Sign in or create your account',
};

export default function AuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
